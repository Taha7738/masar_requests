# التقرير الفني: فصل المهمة الرسمية وتسوية الحضور بالساعات — V20

## 1. القرار المعماري

تم فصل المسؤوليات إلى ثلاثة مستندات واضحة:

```text
Official Duty Request
= المعاملة الإدارية للمهمة: المدة، الموقع، التفسير، التقرير، البديل والاعتمادات.

Attendance Request
= مستند HRMS القياسي لتسجيل أو تصحيح الحضور اليومي عند الحاجة.

Attendance
= النتيجة اليومية النهائية، مع حقول تدقيق الساعات المرتبطة بالمهمة.
```

لم يعد تطبيق `masar_requests` يربط أي JavaScript أو `has_permission` أو `permission_query_conditions` أو Controller مخصص أو `doc_events` مع `Attendance Request`.

يوجد عليه رابط تقني مخفي واحد فقط:

```text
custom_official_duty_request
```

وظيفته منع التكرار والتتبع، ولا يغير واجهة أو منطق HRMS القياسي.

## 2. آلية البصمات

لا تتطلب المهمة بصمة خاصة عند الخروج للمهمة أو العودة منها. النظام يستخدم:

- بصمة الدخول العادية عند أول دخول فعلي للمؤسسة.
- بصمة الانصراف العادية عند آخر خروج فعلي.
- الفترة المعتمدة داخل `Official Duty Request` لتغطية وقت المهمة.

لا ينشئ الكود أي `Employee Checkin`، ولا يغير أي بصمة قائمة.

### مطابقة إعدادات Shift Type القياسية

لا يفترض المحرك طريقة ثابتة لقراءة البصمات. بل يقرأ من الوردية نفسها:

```text
determine_check_in_and_check_out
working_hours_calculation_based_on
```

ويدعم التركيبات القياسية الأربع:

1. التناوب بين IN وOUT + أول بصمة وآخر بصمة.
2. التناوب بين IN وOUT + كل زوج صحيح.
3. الاعتماد الصارم على `log_type` + أول IN وآخر OUT.
4. الاعتماد الصارم على `log_type` + كل زوج IN/OUT صحيح.

وبذلك تكون طريقة تكوين الفترات الفعلية متوافقة مع سياسة الوردية المستخدمة في الحضور التلقائي، مع بقاء دمج فترة المهمة ومنع الاحتساب المزدوج مسؤولية محرك V20.

## 3. طريقة حساب الساعات

لكل يوم يحدد النظام:

```text
Shift Window             = فترة الوردية
Official Duty Intervals  = فترة المهمة المعتمدة
Physical Intervals       = الفترات المثبتة بالبصمات العادية
Credited Intervals       = اتحاد المهمة والحضور الفعلي دون تكرار
Uncovered Hours          = الوردية ناقص الساعات المحتسبة
```

وقت المهمة يُطرح من تقرير الساعات الفعلية داخل المؤسسة، ثم يدمج مع الحضور لمنع احتساب الساعة مرتين.

## 4. السيناريوهات المعتمدة

### أ. مهمة يوم كامل

```text
الوردية: 08:00–16:00
المهمة: 08:00–16:00
البصمات: لا توجد
```

النتيجة بعد الاعتماد ونهاية الوردية:

```text
Attendance = Present
Official Duty Hours = 8
Physical Working Hours = 0
Credited Working Hours = 8
```

عند عدم وجود سجل حضور، ينشئ المحرك `Attendance Request` قياسياً ليوم واحد وبسبب `On Duty`، ثم يربطه بالمهمة.

### ب. مهمة من بداية الدوام ثم وصول الموظف للمؤسسة

```text
الوردية: 08:00–16:00
المهمة: 08:00–12:00
IN عند الوصول: 12:00
OUT النهائي: 16:00
```

النتيجة:

```text
Official Duty Hours = 4
Physical Working Hours = 4
Credited Working Hours = 8
Attendance = Present
```

لا توجد حاجة إلى بصمة خروج خاصة بالمهمة؛ بصمة 12:00 هي أول دخول عادي للمؤسسة في ذلك اليوم.

### ج. مهمة وسط الدوام مع بصمتي البداية والنهاية العاديتين

```text
IN = 08:00
المهمة = 11:00–13:00
OUT = 16:00
```

النتيجة:

```text
Physical Working Hours = 6
Official Duty Hours = 2
Credited Working Hours = 8
```

رغم أن أول وآخر بصمة تحيطان باليوم كله، يطرح النظام فترة المهمة من الساعات الفعلية كي لا تصبح النتيجة 10 ساعات.

### د. مهمة في آخر الدوام مع عدم وجود بصمة انصراف

```text
IN = 08:00
المهمة = 14:00–16:00
OUT = غير موجودة
```

الطلب المعتمد يحدد أن المغادرة للمهمة كانت الساعة 14:00، لذلك:

```text
Physical Working Hours = 6
Official Duty Hours = 2
Credited Working Hours = 8
Missing Checkout Explained = Yes
Attendance = Present
```

لا تُنشأ بصمة OUT وهمية.

### هـ. مهمة وسط الدوام مع بصمة دخول فقط

```text
IN = 08:00
المهمة = 11:00–13:00
لا توجد بصمة عودة أو انصراف نهائي
```

يحسب النظام فقط:

```text
08:00–11:00 = حضور مثبت حتى بداية المهمة
11:00–13:00 = مهمة رسمية
13:00–16:00 = غير مغطاة
```

فتصبح الحالة `Manual Review`، ولا يحول اليوم إلى حضور كامل.

### و. مهمة من بداية الدوام تنتهي قبل نهاية الوردية دون وصول مثبت

إذا كانت المهمة 08:00–12:00 ولا توجد بصمة وصول بعد ذلك، تغطي المعاملة أربع ساعات فقط. تبقى الساعات 12:00–16:00 غير مغطاة وتنتقل الحالة إلى مراجعة الموارد البشرية.

### ز. التوثيق بأثر رجعي

تاريخ إنشاء الطلب لا يحدد يوم الحضور. عند إنشاء واعتماد الطلب لاحقاً، يعالج المحرك `from_date` و`to_date` الفعليين للمهمة، ولذلك يمكن تصحيح `Absent` أو `Half Day` سابق بعد اكتمال الاعتماد.

إن كانت قسيمة الراتب قد تم اعتمادها قبل التصحيح، يلزم إلغاء القسيمة وإعادة احتسابها أو تنفيذ تسوية مالية وفق سياسة المؤسسة.

## 5. حماية البيانات والتعارضات

- يمنع إنشاء مهمتين متداخلتين للموظف نفسه.
- المهمة بالساعات محصورة في تاريخ واحد.
- وقت المهمة يجب أن يقع داخل الوردية الفعلية، مع دعم الورديات الليلية.
- لا ينشئ طلب حضور مكرراً لنفس المهمة واليوم.
- يستخدم قفل صف قاعدة البيانات لمنع عاملين خلفيين من إنشاء تسويتين متزامنتين.
- وجود إجازة معتمدة أو `Work From Home` أو طلب حضور مستقل مرتبط بالسجل يحول الحالة إلى `Manual Review` بدلاً من التعديل التلقائي.
- إعادة تشغيل التسوية آمنة؛ تحفظ الحالة الأصلية مرة واحدة.
- عند إلغاء المهمة، يُلغى طلب الحضور الذي أنشأته المهمة أو تعاد حالة سجل الحضور السابق.

## 6. المعالجة الزمنية

إذا اعتمد الطلب قبل نهاية الوردية:

```text
processing_status = Waiting for Shift End
```

تعمل مهمة دورية كل ساعة لإعادة الفحص بعد نهاية الوردية وسماحية الانصراف ووصول `Last Sync of Checkin` عند تفعيل الحضور التلقائي. كما يتوفر زر `Reconcile Attendance` لمدير الموارد البشرية ومدير النظام.

## 7. سير العمل

```text
Draft
├─ مع بديل → Waiting for Substitute Approval
│              ├─ اعتماد البديل → Waiting for Direct Manager Approval
│              └─ رفض البديل → Draft مع تفريغ البديل
└─ دون بديل → Waiting for Direct Manager Approval

Waiting for Direct Manager Approval
→ Waiting for HR Manager Approval
→ Approved / Submitted
```

المسؤول المباشر يستطيع اعتماد الطلب أثناء انتظار البديل، والموارد البشرية تستطيع الاعتماد النهائي أو الرفض من المراحل النشطة وفق التخصيص السابق.

## 8. ما بقي قياسياً

`Attendance Request` عاد إلى HRMS القياسي من حيث:

- Controller.
- JavaScript.
- الصلاحيات.
- أسباب `On Duty` و`Work From Home`.
- نصف اليوم.
- تضمين العطلات.
- Submit وCancel وإنشاء/إلغاء Attendance.

## 9. حدود الأتمتة المقصودة

لا يفترض المحرك حضور الموظف في فترة لا تغطيها بصمة أو مهمة معتمدة. لذلك `Manual Review` نتيجة صحيحة وآمنة، وليست خطأً برمجياً، عندما توجد فجوة زمنية غير مثبتة.
