# تقرير الإصدار V21 — أوقات الوردية المتغيرة حسب يوم الأسبوع

## 1. نطاق العمل

بُني هذا الإصدار فوق **Masar Requests V20** الذي يتضمن فصل المهمة الرسمية عن `Attendance Request`، ومعالجة المهمة الرسمية بالساعات، وتسوية إجازة ربع اليوم والإجازة بالساعات.

لم تُعدّل ملفات HRMS الأصلية. جميع التغييرات داخل تطبيق `masar_requests`، مع طبقة توافق محمية تتوقف عن تطبيق الميزة عند اختلاف واجهة HRMS بدل تعطيل النظام القياسي.

## 2. المشكلة السابقة

كان التخصيص القديم يحاول تعديل دالة `get_employee_shift` من وحدة غير صحيحة، ويغير مفاتيح لا يعتمد عليها HRMS في نافذة الوردية الفعلية. لذلك كان جدول الأيام يظهر ويحفظ، لكن `Employee Checkin` و`Auto Attendance` قد يستمران باستخدام `start_time` و`end_time` القياسيين.

كما كانت واجهة `Shift Type` تمسح جدول الأيام عند تغيير `Holiday List`، مما قد يؤدي إلى فقد التعديلات اليدوية.

## 3. التصميم الجديد

أضيفت الحقول التالية إلى `Shift Type`:

- `custom_enable_variable_shift_times`: تفعيل الأوقات المتغيرة.
- `custom_shift_times_effective_from`: تاريخ بدء اختياري.
- `custom_shift_times`: جدول يحتوي صفًا لكل يوم.

ويحتوي جدول `Shift Time Table` على:

- يوم الأسبوع.
- وقت البداية.
- وقت النهاية.
- عدد ساعات الوردية المحسوب.

تبقى `Holiday List` مسؤولة عن العطلات الأسبوعية والرسمية. وجود وقت ليوم الجمعة مثلًا لا يجعله يوم عمل إذا كان التاريخ عطلة في قائمة العطلات.

## 4. نقطة التكامل المركزية

يغلف الإصدار V21 دالة HRMS المركزية:

```python
hrms.hr.doctype.shift_assignment.shift_assignment.get_shift_details
```

وهي التي تُرجع:

- `start_datetime`
- `end_datetime`
- `actual_start`
- `actual_end`

بذلك تصل أوقات اليوم الصحيحة إلى تحديد الوردية والبصمة، بدل تعديل قيم شكلية بعد انتهاء الحساب.

## 5. المستندات والعمليات التي أصبحت تستخدم الوقت اليومي

### Employee Checkin

عند إنشاء بصمة، يحدد HRMS الوردية ووقت بدايتها ونهايتها من النافذة الخاصة بيوم البصمة. تُخزن القيم الصحيحة في:

- `shift_actual_start`
- `shift_actual_end`
- `shift_start`
- `shift_end`

### Auto Attendance

يستمر HRMS القياسي في تجميع البصمات وحساب ساعات العمل. وبما أن البصمات تحمل نافذة اليوم الصحيحة، تعمل قواعد:

- التأخير.
- الخروج المبكر.
- ساعات العمل.
- الحاضر ونصف اليوم والغياب.

وفق التوقيت الخاص بذلك اليوم.

### الأيام التي لا توجد فيها بصمات

غُلّفت دالتا تعليم الغياب والنصف الآخر كغياب، لأن HRMS القياسي كان يبني وقت الفحص من `Shift Type.start_time` العام. يستخدم V21 بداية اليوم المحددة في الجدول، ثم ينفذ بقية السلوك القياسي.

### Last Sync of Checkin

تم تصحيح تحديد آخر نهاية وردية قابلة للمزامنة، خصوصًا عند اختلاف نهاية الخميس عن بقية الأيام أو عند استخدام ورديات ليلية.

### Shift Assignment

- تحديد وردية الموظف يستخدم النافذة اليومية المركزية.
- تقويم Shift Assignment يعرض وقت كل يوم بصورة صحيحة.
- التحقق من تداخل أنواع الورديات يستخدم جدول الأسبوع بطريقة محافظة وآمنة.

### Leave Application

ربع اليوم والإجازة بالساعات يجلبان ساعات الوردية من المحلل المركزي. مثال:

- الأحد 08:00–16:00 = 8 ساعات.
- الخميس 08:00–14:00 = 6 ساعات.
- إجازة ساعتين يوم الخميس = `2 / 6 = 0.3333` يوم، وليس `2 / 8`.

### Official Duty Request

المهمة الرسمية تعتمد بداية ونهاية الوردية الفعلية ليوم المهمة، بما يشمل:

- مهمة يوم كامل.
- مهمة من بداية الدوام.
- مهمة في نهاية الدوام.
- مهمة خلال وردية ليلية.

### Attendance وSalary Slip

`Attendance` يستقبل النتيجة من البصمة والحضور التلقائي أو من محركات التسوية. `Salary Slip` يستمر في الاعتماد على `Attendance` وكسور الإجازة وفق تصميم V20؛ لذلك لا يوجد منطق رواتب مكرر داخل تخصيص الوردية.

## 6. دعم الوردية الليلية

إذا كان يوم الجمعة:

```text
22:00 → 06:00
```

فإن الوردية تبدأ يوم الجمعة وتنتهي صباح السبت. عند بصمة الساعة 02:00 صباح السبت، يختار النظام نافذة الجمعة السابقة، وليس وردية السبت التالية.

## 7. الحماية عند تعديل الجدول

- جميع الأيام السبعة إلزامية عند تفعيل الميزة.
- يمنع تكرار اليوم.
- يمنع تساوي وقت البداية والنهاية.
- يمنع أن تصبح نافذة الوردية مع هوامش الدخول والخروج 24 ساعة أو أكثر.
- يمنع تغيير جدول نافذ عند وجود بصمات غير معالجة.
- لا تمس البصمات المرتبطة أصلًا بسجل حضور.
- أثناء ترقية V21 يعاد حساب نافذة البصمات القديمة غير المعالجة فقط، حتى لا تبقى بالقيم القياسية السابقة.

## 8. مقاومة تعارض تحديثات HRMS

لا يمكن ضمان التوافق مع كل إصدار مستقبلي غير معروف، لكن V21 يقلل مخاطر التحديث بالآتي:

1. لا يعدل أي ملف داخل تطبيق HRMS.
2. يفحص توقيع الدوال قبل التغليف.
3. يغلف أقل عدد من النقاط المركزية.
4. إذا تغيرت واجهة HRMS، يبقى السلوك القياسي فعالًا وتتعطل ميزة الأيام المتغيرة بأمان.
5. توجد دالة تدقيق:

```bash
bench --site masar-test.local execute \
  masar_requests.overrides.shift_type.audit_shift_times_patch
```

6. فحص `preflight` يعتبر الموقع غير جاهز إذا لم تكن طبقة التوافق فعالة.

بعد أي تحديث لـHRMS يجب تشغيل فحص التوافق واختبارات القبول قبل معالجة الحضور.

## 9. تصحيحات أخرى اكتُشفت أثناء المراجعة

### ملف Material Request JavaScript

كان ملف V20:

```text
masar_requests/public/js/material_request.js
```

يحتوي بالخطأ على كود `Leave Application` بدل أحداث `Material Request`. تم استبداله بالملف الصحيح، مع الحفاظ على:

- صلاحية HR User للقراءة والطباعة فقط.
- حماية القيم بعد مراحل الاعتماد.
- إظهار السعر والمبلغ لطلبات الشراء في الصف الموسع.
- إخفائهما في طلب الصرف الداخلي وفي العرض المختصر.

### توحيد محلل الوردية

أزيل تكرار منطق جلب أوقات الأيام من:

- `official_duty_engine.py`
- `leave_application_partial_leave.py`
- JavaScript الخاص بطلب الإجازة

وأصبحت جميعها تستدعي مصدرًا مركزيًا واحدًا، مما يمنع اختلاف نتيجة كل مستند عن الآخر.

### الترحيل والكاش

أضيف Patch V21، وتوسعت عملية مسح الكاش لتشمل:

- Shift Type
- Shift Assignment
- Employee Checkin
- Attendance
- Leave Application
- Official Duty Request

## 10. ملاحظات تشغيلية

- حدود `working_hours_threshold_for_half_day` و`working_hours_threshold_for_absent` في HRMS تبقى قيمًا مطلقة بالساعات، وليست نسبًا تلقائية من طول كل يوم. يجب ضبطها بما يلائم أقصر يوم عمل في نوع الوردية، أو استخدام نوع وردية مستقل عندما تتطلب المؤسسة حدودًا مختلفة جوهريًا.
- لا يعاد احتساب سجلات حضور معتمدة قديمة تلقائيًا. الترحيل يعيد حساب البصمات غير المرتبطة بحضور فقط.
- تاريخ `Variable Times Effective From` يسمح بفصل الجدول الجديد عن الفترات التاريخية عند الحاجة.

## 11. مستوى التحقق

تم تنفيذ:

- تجميع جميع ملفات Python.
- فحص بناء جميع ملفات JavaScript.
- قراءة جميع JSON وCSV.
- فحص Shell syntax.
- اختبار مستقل لتوافق الترقيع وتراجع السلوك القياسي.
- اختبار وردية نهارية وليلية واختيار نافذة اليوم السابق بعد منتصف الليل.
- فحص مسارات hooks وملفات Patch.
- مقارنة كاملة مع V20 وحصر الملفات المعدلة.

لم يتم تشغيل `bench migrate` أو اختبارات Frappe المتصلة بقاعدة بيانات موقع حقيقي داخل بيئة إعداد الحزمة. لذلك يجب تثبيت الإصدار أولًا على `masar-test.local` وتنفيذ اختبارات القبول قبل الإنتاج.
