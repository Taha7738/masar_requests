# ملخص التنفيذ النهائي — V20

1. تم اعتماد النسخة المرفوعة بتاريخ 30-7-2026 أساساً وحيداً للتعديل.
2. تم إنشاء `Official Duty Request` كمستند قياسي مستقل داخل التطبيق.
3. نُقلت حقول المهمة وWorkflow والصلاحيات والإشعارات والطباعة إلى المستند الجديد.
4. أُعيد `Attendance Request` إلى HRMS القياسي، مع رابط تقني مخفي واحد فقط.
5. تم إنشاء محرك تسوية بالساعات يجمع فترات المهمة والبصمات العادية دون تكرار، ويطابق إعدادات Shift Type لطريقة IN/OUT وحساب ساعات العمل.
6. لا ينشئ المحرك بصمة دخول أو خروج وهمية.
7. يدعم المهمة في بداية الدوام، وسطه، نهايته، اليوم الكامل، الأثر الرجعي، العطلات والورديات الليلية.
8. الفترات غير المثبتة لا تُغطى تلقائياً؛ تتحول إلى `Manual Review`.
9. تم إصلاح انعكاس ربع اليوم والإجازة بالساعات على `Attendance` والرواتب بعد دمج البصمات مع فترة الإجازة، مع تحويل الفجوات إلى `Manual Review`.
10. تم إنشاء Patch يرحل بيانات المهمة القديمة قبل حذف تخصيصات Attendance Request.
11. التنظيف محدد ببصمات التطبيق ولا يحذف تخصيصات أخرى غير معروفة.
12. أضيفت تعليقات عربية وإنجليزية في الدوال والأجزاء الجديدة، وترجمات عربية للنصوص والحقول الجديدة.
13. أضيفت بوابة أمان تمنع احتساب Salary Slip عند وجود إجازة جزئية غير مسوّاة، مع اختبارات بنيوية وحسابية للسيناريوهات الأساسية.

## حدود التحقق المنفذ خارج موقع Frappe

تم التحقق من صياغة Python وJavaScript وJSON وCSV وShell، ومن سلامة ترتيب حقول DocType وعدم وجود أخطاء whitespace. لم تُشغّل في بيئة الإعداد عملية `bench migrate` أو اختبارات متصلة بقاعدة بيانات الموقع، ولذلك يظل اختبار القبول على `masar-test.local` إلزامياً قبل الإنتاج.
