# التقرير الفني المنفصل: ربع اليوم والإجازة بالساعات — V20

## 1. المشكلة التي عولجت

منطق HRMS القياسي يمثل الحضور اليومي أساساً بحالات يوم كامل أو نصف يوم. أما التخصيص السابق فكان يحسب ربع اليوم والإجازة بالساعات داخل `Leave Application` ودفتر الإجازات، لكنه لم يكن يضمن إنشاء أو تسوية سجل `Attendance` بدقة، كما كان من الممكن أن يتعامل الراتب مع طلب LWP/PPL الجزئي كمكافئ يوم كامل لأنه ليس `half_day` قياسياً.

## 2. القرار التنفيذي

أُبقي منطق اليوم الكامل ونصف اليوم القياسي كما هو، وأضيف مسار مستقل فقط عندما يكون:

```text
quarter_day = 1
أو
is_hourly = 1
```

المسار الجديد:

```text
Leave Application
→ حساب الساعات وكسر اليوم الدقيق
→ خصم الرصيد بالكسر الدقيق
→ انتظار نهاية الوردية ومزامنة البصمات
→ دمج البصمات الفعلية مع فترة الإجازة
→ Attendance Reconciliation
→ Salary Slip بالكسر الدقيق
```

لا ينشئ هذا المسار أي `Employee Checkin` ولا يعدل البصمات الأصلية.

ويستخدم نفس طريقتي الوردية القياسيتين لتفسير البصمات وحساب الساعات (`determine_check_in_and_check_out` و`working_hours_calculation_based_on`). لذلك لا يفرض الإعداد المخصص طريقة حساب تختلف عن إعداد `Shift Type` الفعلي.

## 3. لماذا لا يُنشأ Attendance فور اعتماد الطلب؟

قد يُعتمد طلب الإجازة قبل نهاية الوردية. إنشاء `Attendance` Submitted في تلك اللحظة قد يمنع `Auto Attendance` من معالجة البصمات الفعلية أو ربطها بصورة سليمة. لذلك تصبح حالة الطلب:

```text
Waiting for Shift End
```

ثم تعيد مهمة مجدولة كل ساعة المحاولة بعد:

1. انتهاء الوردية، بما في ذلك سماحية الانصراف.
2. وصول `Last Sync of Checkin` عند تفعيل الحضور التلقائي.

## 4. آلية التغطية بالساعات

لكل طلب جزئي يحسب النظام:

```text
Shift Window       = كامل زمن الوردية
Leave Interval     = فترة الإجازة المأذونة
Physical Intervals = فترات الحضور المثبتة بالبصمات الفعلية
Credited Intervals = اتحاد الحضور الفعلي والإجازة دون احتساب مزدوج
Uncovered Hours    = الوردية ناقص الفترات المحتسبة
```

### قاعدة النزاهة

لا يحول النظام `Absent` أو `Half Day` إلى `Present` لمجرد وجود إجازة لساعتين أو ربع يوم. يجب أن تغطي البصمات الفعلية مع فترة الإجازة كامل الوردية ضمن سماحية دقيقة واحدة.

عند اكتمال التغطية:

```text
custom_partial_attendance_status = Reconciled
Attendance.status = Present
```

عند وجود فجوة:

```text
custom_partial_attendance_status = Manual Review
```

ويظل الوضع اليومي الموجود دون ترقية تلقائية إلى `Present`.

## 5. أمثلة عملية

### أ. ربع يوم في نهاية الدوام

```text
الوردية: 08:00–16:00
البصمة: IN الساعة 08:00
الإجازة: 14:00–16:00
OUT: غير موجودة
```

النتيجة:

```text
ساعات فعلية = 6
ساعات إجازة = 2
الساعات المحتسبة = 8
الحالة = Reconciled / Present
```

الطلب يثبت بداية الإجازة، ولا تُنشأ بصمة انصراف وهمية.

### ب. ربع يوم في بداية الدوام

```text
الإجازة: 08:00–10:00
IN عند الوصول: 10:00
OUT النهائي: 16:00
```

النتيجة: ساعتان إجازة + ست ساعات فعلية = تغطية كاملة.

### ج. إجازة وسط الدوام مع بصمتي البداية والنهاية

```text
IN = 08:00
الإجازة = 11:00–13:00
OUT = 16:00
```

تطرح فترة الإجازة من تقرير الساعات الفعلية ثم تدمج معها، فلا تصبح النتيجة عشر ساعات. الساعات المحتسبة تظل ثماني ساعات فقط.

### د. إجازة جزئية مع غياب غير مبرر

```text
الوردية: 08:00–16:00
الإجازة: 14:00–16:00
لا توجد بصمات
```

الإجازة تبرر ساعتين فقط، وتبقى ست ساعات غير مغطاة. النتيجة `Manual Review`، ولا يُنشأ حضور كامل.

## 6. الحقول المحفوظة على Attendance

```text
custom_partial_leave_application
custom_partial_leave_type
custom_partial_leave_hours
custom_partial_leave_day_fraction
custom_partial_leave_from_time
custom_partial_leave_to_time
custom_partial_leave_reconciliation_status
custom_partial_leave_previous_status
custom_partial_leave_previous_half_day_status
custom_partial_leave_previous_leave_type
custom_partial_leave_previous_leave_application
```

هذه حقول تدقيق وربط، ولا تغير سجلات البصمة.

## 7. أمثلة الكسور

### ربع يوم

```text
وردية 8 ساعات
إجازة ساعتان
كسر اليوم = 2 / 8 = 0.25
```

### إجازة ثلاث ساعات

```text
وردية 8 ساعات
إجازة 3 ساعات
كسر اليوم = 3 / 8 = 0.375
```

يُحفظ الكسر في `total_leave_days` ويستخدم في رصيد الإجازة وفي LWP/PPL.

## 8. احتساب الراتب

تمت إضافة `CustomSalarySlip` مع إبقاء الحساب القياسي لليوم الكامل ونصف اليوم.

### Payroll Based On = Leave Application

يصحح النظام المكافئ القياسي من يوم كامل إلى الكسر الدقيق الموجود في `total_leave_days`.

### Payroll Based On = Attendance

يبقى سجل اليوم `Present` بعد اكتمال التسوية، ثم يضاف كسر LWP/PPL الدقيق بصورة مستقلة دون تغيير احتساب الغياب القياسي.

### بوابة أمان الرواتب

قبل حساب الراتب، يفحص النظام كل إجازة ربع يوم أو بالساعات داخل فترة القسيمة. إذا كانت حالتها:

```text
Pending
Waiting for Shift End
Manual Review
Failed
```

يوقف حساب القسيمة برسالة واضحة حتى تصبح `Reconciled`. هذا يمنع الجمع الخاطئ بين غياب يوم كامل وكسر إجازة جزئية في الراتب نفسه.

## 9. الإلغاء والاستعادة

- إذا أنشأ الطلب سجل Attendance خاصاً به، يُلغى السجل عند إلغاء الطلب.
- إذا كان سجل Attendance موجوداً قبل الطلب، تحفظ حالته الأصلية ثم تعاد عند الإلغاء.
- إذا أُلغي الطلب قبل نهاية الوردية، تتحول حالة المعالجة إلى `Cancelled` دون إنشاء Attendance.
- لا تُلغى أو تُحذف أي بصمة.

## 10. التداخلات والضوابط

- يمنع اختيار نصف يوم وربع يوم وإجازة بالساعات معاً.
- الطلب الجزئي محصور في تاريخ واحد.
- الوقت يجب أن يقع داخل الوردية، مع دعم الوردية الليلية.
- يمنع ربط Attendance بطلبين جزئيين مختلفين.
- وجود `On Leave` مستقل أو ساعات غير مغطاة يتطلب مراجعة الموارد البشرية.
- يعاد فحص حالات `Manual Review` دورياً، بحيث يمكن حلها بعد وصول بصمات متأخرة.

## 11. اختبارات القبول الإلزامية

1. ربع يوم في بداية الوردية مع IN عند الوصول وOUT نهائي.
2. ربع يوم في نهاية الوردية مع IN واحد وعدم وجود OUT.
3. إجازة وسط الوردية مع IN/OUT طبيعيين ومنع الاحتساب المزدوج.
4. إجازة جزئية بلا بصمات كافية والتأكد من `Manual Review`.
5. وصول بصمة متأخرة ثم إعادة المعالجة والتحول إلى `Reconciled`.
6. LWP وPPL في طريقتي احتساب الرواتب.
7. محاولة إعداد الراتب قبل التسوية والتأكد من إيقافه.
8. الإلغاء واستعادة Attendance السابق.
9. التأكد من عدم تغير اليوم الكامل ونصف اليوم القياسيين.

## 12. ملاحظة تشغيلية

يجب إكمال تسوية الحضور الجزئي قبل اعتماد مسير الرواتب. بعد Submit لقسيمة الراتب، أي تعديل لاحق في الحضور لا يعيد احتساب القسيمة المعتمدة تلقائياً؛ يلزم إلغاء القسيمة وإعادة إنشائها أو إجراء تسوية مالية وفق سياسة المؤسسة.
