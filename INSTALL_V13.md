# خطوات تطبيق V13

## النسخة الكاملة النظيفة

استبدل تطبيق `masar_requests` بالنسخة الكاملة، ثم نفّذ:

```bash
bench --site اسم_الموقع migrate
bench --site اسم_الموقع clear-cache
bench build --app masar_requests
bench restart
```

ثم حدّث المتصفح تحديثاً كاملاً:

```text
Ctrl + Shift + R
```

## نسخة الملفات المعدلة فقط

انسخ الملفات مع المحافظة على المسارات، ثم شغّل ملف التنظيف:

```bash
bash cleanup_obsolete_attendance_files.sh /path/to/frappe-bench/apps/masar_requests
```

ثم نفّذ أوامر `migrate` و`build` الموضحة أعلاه.

## اختبارات قبول مقترحة

1. إنشاء طلب مهمة دون بديل، وإرساله مباشرة إلى المسؤول المباشر.
2. إنشاء طلب مهمة مع بديل، والتأكد أن المدير يراه فوراً ويمكنه الاعتماد قبل البديل.
3. رفض البديل والتأكد من عودة الطلب للموظف في Draft مع إمكانية اختيار بديل آخر.
4. التأكد أن تقرير الإنجاز مطلوب عند الإنشاء وأنه لا توجد إجراءات مستقلة للتقرير.
5. الطباعة بعد اعتماد المدير وHR، والتحقق من تطابق «جهة الاعتماد» و«اعتماد التقرير».
6. الاعتماد النهائي المبكر والتأكد من ظهور «تم الاعتماد من قبل» في الخانات المتجاوزة.
7. الدخول بمستخدم يحمل HR User فقط: مشاهدة وطباعة الطلبات الثلاثة دون ظهور أزرار اعتماد أو إمكانية حفظ.
